---
name: User Profile — Furigana Project
description: User is a solo full-stack developer building the Furigana MVP; works with React Router v7 SSR, TypeScript strict mode, and AI-powered features
type: user
---

The user is a solo full-stack developer working on the Furigana MVP project — an AI-powered Japanese reading assistant. They are comfortable with:

- React Router v7 (SSR, loaders, actions, fetchers)
- TypeScript 5.8 in strict mode (exactOptionalPropertyTypes, noUncheckedIndexedAccess)
- Tailwind CSS v4 (CSS-first config)
- shadcn/ui component library
- AI API integration (OpenAI SDK)
- Zod schema validation
- Vitest and Playwright for testing

The user works from milestone-based PRDs and roadmaps stored in `docs/plans/{date}/`. Sub-PRDs for implementation are stored in `.taskmaster/docs/plans/{date}/prd.md`.
